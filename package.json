{
  "name": "html-customs-broker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.js",
    "validate:data": "node scripts/validate-data.js",
    "build": "npm run validate:data && node scripts/build.js",
    "release": "npm run build && cd dist && zip -qr ../html-broker-dist.zip ."
  }
}
